const { MongoClient } = require('mongodb'); require('dotenv').config(); MongoClient.connect(process.env.MONGODB_URI).then(async (client) => { const db = client.db('vibes_lab'); await db.collection('people').updateOne( { 'publications.title': /Position and time localization/i }, { $set: { 'publications.$.url': 'https://mail.google.com/mail/u/0/?ogbl#inbox' } } ); console.log('Successfully added URL to MongoDB!'); process.exit(0); });
